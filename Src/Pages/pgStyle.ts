import {
  StyleSheet,
} from 'react-native';

export const Pgstyle = StyleSheet.create({
    clientArea:{
        paddingLeft: 20,
        paddingRight: 20,
        paddingBottom: 20,
        gap: 14,
    }
});